<div class="post-list">
  {{#each posts}}
    <article class="post">
      <h2>{{title}}</h2>
      <p>Posted by {{user.username}} on {{format_date createdAt}}</p>
      <p>{{content}}</p>
    </article>
  {{/each}}
</div>